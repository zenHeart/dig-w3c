# 文本标记

**详解文本标记标签的种类及作用**

----


## 概述
html 标签大部分为内容呈现而存在,所以文本标记标签占据了大部分。
这里的文本标记标签重点是控制文本的语义功能而非视图呈现(虽然也存在视图差异)。

例如 `strong` 标签表示需要强调的文本片段,而呈现效果体现为字体的加粗和变黑。

> 在学习任何 HTML 标签是重点考虑语义而非视图,保证页面标签结构能够完整体现页面的信息结构